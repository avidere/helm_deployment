apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pythonapp_chart.fullname" . }}-test-connection"
  labels:
    {{- include "pythonapp_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pythonapp_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
