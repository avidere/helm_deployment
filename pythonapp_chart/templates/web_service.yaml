apiVersion: v1
kind: Service
metadata:
  name: {{ include "pythonapp_chart.fullname" . }}
  labels:
    {{- include "pythonapp_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pythonapp_chart.selectorLabels" . | nindent 4 }}
